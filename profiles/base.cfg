[buildout]
parts =
    django

extends =
    versions.cfg

versions = versions

eggs =

[django]
recipe = djangorecipe
settings = development
eggs = ${buildout:eggs}
extra-paths =
project = shopping_search
